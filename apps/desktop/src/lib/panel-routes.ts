/*
 * Home do painel 1200x800.
 *
 * O chat live mora na ilha; o painel é o workspace — regras, documentos,
 * procedimentos, pessoas, modelos. Abrir o painel sem rota explícita cai
 * aqui, nunca num /chat vazio.
 */
export const PANEL_HOME_ROUTE = "/settings/workspace";
